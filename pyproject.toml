[build-system]
requires = ["setuptools >= 69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mimlos"
dynamic = ["version"]
description = "Municipal Inventory seisMic LOSs assessment framework"
readme = """
A repository for the seismic loss and recovery analysis of a municipal building inventory in the Canadian context, with framework for modelling and analysing multi-degree-of-freedom systems for response characterisation of global building classes powered by OpenQuake VMTK
Copyright (C) 2025 GEM Foundation
"""
license = { text = "AGPL-3.0-only" }

authors = [
    { name = "Huy G. Pham", email = "huy.pham@civil.ubc.ca" },
    { name = "GEM Foundation", email = "risk@globalquakemodel.org" },
]
maintainers = [
    { name = "Huy G. Pham", email = "huy.pham@civil.ubc.ca" },
    { name = "GEM Foundation", email = "risk@globalquakemodel.org" },
]

keywords = [
    "risk",
    "seismic",
    "risk seismic",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Operating System :: OS Independent",
    "Programming Language :: Python ",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering",
]

requires-python = ">=3.11, <3.14"
dependencies = [
	"seaborn >= 0.13.0",
    "ipykernel >= 4.8.0",
    "ipython >= 6.2.0",
    "jupyter_client >= 5.0.0",
    "notebook >= 5.0.0",
    "pyproj >= 3.7.2",
    "scipy >= 1.15.3",
    "matplotlib >= 3.10.1",
    "numpy >= 2.2.6",
    "numba >= 0.61.2",
    "pandas >= 2.2.3",
    "statsmodels >= 0.14.4",
    "pydantic >= 2.10.6 ",
    "pelicun >= 3.9",
    "atc138 @ git+https://github.com/hgp297/Functional-Recovery-Python.git",
    "openpyxl"
]


[project.urls]
Homepage = "https://github.com/GEMScienceTools/vulnerability-toolkit"
Repository = "https://github.com/GEMScienceTools/vulnerability-toolkit"
Documentation = " "

[tool.setuptools.packages.find]
where = ["."]
include = ["openquake*","mimlos*"]

[tool.setuptools.exclude-package-data]
"*" = [
	"tests",
	"tests.*"
]

[tool.setuptools.dynamic]
version = {attr = "mimlos.mimlos.__version__"}
